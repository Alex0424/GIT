# GIT CLI UI

to know on what branch you are on add this content to ~/.bashrc file

`vim ~/.bashrc`
```
parse_git_branch() {
  git branch 2>/dev/null | grep '*' | sed 's/* //'
}

export PS1="\u@\h \W \[\033[32m\]\$(parse_git_branch)\[\033[00m\] $ "
```
